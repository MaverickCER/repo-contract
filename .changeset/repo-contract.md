---
"repo-contract": patch
---

### Changed Files

<!-- repo-contract:changeset-docs:start:hash=3ef8 created-frontmatter=true -->

- **.dependency-cruiser.cjs** (added, +445/-0): dependency-cruiser ruleset backing the `architecture` check -- encodes this repo's layering as forbidden-dependency rules (no circular deps, `src/execution` must not import `src/policy` per ADR 0001, `src/` must not import `scripts/`, checks/scripts must not reach into `src/` internals), each with its severity and rationale inline.
- **.gitattributes** (added, +18/-0): Pins LF line endings for source and fixture files so Windows checkouts don't break API-baseline hash comparisons or trip prettier-in-CI (see the Windows CI notes).
- **.github/pull_request_template.md** (added, +51/-0): PR body scaffold: what-changed-and-why, deliberately-not-addressing, and the Code Owner residual-risk checklist covering what `npm run contract` can't judge mechanically (links CODE_REVIEW.md).
- **.github/workflows/ci.yml** (added, +240/-0): CI pipeline: the modern-Node verification toolchain (typecheck, lint, build, every test category, size), the packed-artifact build, and the `published-floor`/smoke-consumer jobs on the package's `engines.node` floor; least-privilege `permissions`.
- **.github/workflows/release.yml** (added, +76/-0): Release pipeline: a credential-free self-hosting-contract job, then Changesets-driven version/publish with OIDC trusted publishing and provenance.
- **.gitignore** (added, +54/-0): Ignores build output, coverage, generated tool reports, Stryker scratch, `history.json`, and other generated artifacts.
- **.jscpd.json** (added, +10/-0): jscpd copy-paste-detection config for the `duplication` self-check -- ignore globs for node_modules/dist/coverage/reports/.stryker-tmp/.changeset.
- **.markdownlint-cli2.jsonc** (added, +28/-0): markdownlint-cli2 config for the `docs` check: gitignore-aware, MD013 off, with `.changeset/`, generated API reports, and the PR template excluded (rationale inline).
- **.npmrc** (added, +6/-0): Sets `provenance=true` so a provenance attestation is attached on publish whether `npm publish` is invoked directly or wrapped by `changeset publish`.
- **.prettierignore** (added, +23/-0): Excludes generated files, tool reports, and fixtures from Prettier formatting.
- **.prettierrc.json** (added, +11/-0): Prettier configuration -- the repo's formatting baseline enforced by the `format` preset.
- **.repo-contract/api-contract/baseline.api.json** (added, +3657/-0): Committed API Extractor Doc Model snapshot of the public surface -- the authoritative historical contract the `api-contract` check diffs current against (read from git HEAD, never the working tree).
- **.repo-contract/api-contract/baseline.d.ts** (added, +577/-0): Committed untrimmed `.d.ts` rollup of the baseline public surface -- the supporting compiler artifact the TypeChecker assignability probe loads as the "old" declaration text.
- **.repo-contract/api-contract/baseline.meta.json** (added, +9/-0): Baseline provenance/integrity metadata: package name/version, API Extractor version, schema version, and the api.json/d.ts hashes that guard against tampering.
- **.secretlintignore** (added, +17/-0): Paths secretlint skips in the `security-secrets` check (lockfile, fixtures, reports).
- **.secretlintrc.json** (added, +7/-0): secretlint config -- enables `@secretlint/secretlint-rule-preset-recommend` for the `security-secrets` check.
- **CHANGELOG.md** (added, +14/-0): Keep a Changelog-style changelog maintained by Changesets; notes the pre-1.0 versioning caveat.
- **checks/accessibility.ts** (added, +75/-0): `accessibility` check policy -- interprets pa11y's JSON report of `docs/index.html`: `error` findings fail, `warning` warns, `notice` is dropped.
- **checks/adr-governance.ts** (added, +128/-0): `adr-governance` check policy -- classifies the evidence (nothing governed changed / ADR set engaged / valid ADR reference / unsatisfied) and hard-fails a PR touching `src/execution` or `src/policy` with no ADR engagement.
- **checks/api-contract.ts** (added, +151/-0): `api-contract` check policy -- renders the classified public-contract delta and changeset action into a rationale and gates on compatibility impact and schema-version-literal defects.
- **checks/api-docs.ts** (added, +95/-0): `api-docs` check policy -- fails when a committed `docs/api-report/*.api.md` no longer matches the real public surface or still carries an `(undocumented)` marker, naming which report and symbol.
- **checks/architecture.ts** (added, +208/-0): `architecture` check policy -- reduces the combined dependency-graph / test-boundary / ADR-structure evidence and fails only on `error`-severity violations.
- **checks/build.ts** (added, +27/-0): `build` check -- runs `npm run build` as an `isolated: true` scheduling barrier between file-writing checks and read-only checks, with its own pass/fail verdict.
- **checks/changeset-docs.ts** (added, +110/-0): `changeset-docs` check policy -- classifies the per-file table evidence (nothing to document / all described / some rows undescribed) and fails until every changed file carries a real description.
- **checks/coverage.ts** (added, +93/-0): `coverage` check policy -- compares the aggregated union coverage summary against `COVERAGE_THRESHOLDS`, per metric, treating a missing/invalid percentage as a failure.
- **checks/crap.ts** (added, +123/-0): `crap` check policy -- compares crap4ts's per-function CRAP scores against the repo-pinned threshold passed via `--threshold`.
- **checks/docs.ts** (added, +155/-0): `docs` check policy -- interprets the combined markdownlint-cli2 + linkinator JSON evidence; lint errors and broken links fail.
- **checks/lint.ts** (added, +161/-0): `lint` check policy -- interprets combined ESLint + oxlint JSON evidence: errors fail, warnings warn, a tool-infrastructure failure fails.
- **checks/mutation.ts** (added, +406/-0): `mutation` check policy -- requires every applicable Stryker mutant to be `Killed`, cross-checking any comment-ignored mutant against a justified `disable-comments.json` Stryker-domain record; a static mutant skipped by the config-level `ignoreStatic` option is accepted outright and excluded from the applicable count.
- **checks/schema.ts** (added, +92/-0): `schema` check policy -- re-reads every regenerated `schemas/*.schema.json` target off disk and confirms valid JSON carrying the `$id`/`title` the generator's own `TARGETS` declares.
- **checks/security-network.ts** (added, +61/-0): `security-network` check policy -- fails on any prohibited or unverifiable network capability found in `src/**/*.ts` by the AST scan (ADR 0013).
- **checks/shared/require-parsed-output.ts** (added, +32/-0): Shared guard nearly every check policy calls first -- fail with a fixed rationale when parsed JSON output is missing or failed to parse, otherwise return it narrowed to `T`.
- **checks/size.ts** (added, +150/-0): `size` check policy -- matches `scripts/check-size.mjs`'s measured gzip figures against the repo-owned `SIZE_BUDGETS`; regression tripwires for an accidentally-inlined dependency.
- **checks/suppression-governance.ts** (added, +188/-0): `suppression-governance` check policy -- validates `suppressionPolicy` shape and fails on any forbidden or under-justified suppression in the synchronized registry (ADR 0007).
- **checks/test-e2e.ts** (added, +26/-0): `test-e2e` check -- runs the `e2e` Vitest category against the built `dist/` via `run-test-category.mjs`; never receives `--coverage`.
- **checks/test-integration.ts** (added, +9/-0): `test-integration` check -- runs the `integration` Vitest category with `--coverage`; shares `test-unit`'s rationale.
- **checks/test-property.ts** (added, +9/-0): `test-property` check -- runs the `property` Vitest category with `--coverage`; shares `test-unit`'s rationale.
- **checks/test-unit.ts** (added, +16/-0): `test-unit` check -- runs the `unit` Vitest category with `--coverage` via `run-test-category.mjs`; that run is what produces the category's coverage artifact.
- **CODE_OF_CONDUCT.md** (added, +122/-0): Contributor Covenant code of conduct.
- **CODE_REVIEW.md** (added, +1527/-0): The residual-risk review guide referenced by the PR template -- everything a human reviewer must judge beyond what `npm run contract` verifies mechanically.
- **CODEOWNERS** (added, +18/-0): Single-owner-every-path ownership, plus the note that `src/execution`/`src/policy` changes need an ADR (mechanically enforced by `adr-governance`).
- **CONTRIBUTING.md** (added, +112/-0): Contributor workflow: running the contract, adding an ADR, changeset discipline, and the verification-category boundaries.
- **disable-comments.json** (added, +882/-0): The suppression-governance registry -- every lint/Stryker suppression in the tree with its hand-authored justification/alternatives/remediation/category/verificationMethod; auto-synchronized by its own check, so never `git checkout` it blindly.
- **docs/api-report/repo-contract-presets.api.md** (added, +55/-0): Generated API Extractor report for the `repo-contract/presets` entry point -- drift- and completeness-gated by the `api-docs` check.
- **docs/api-report/repo-contract.api.md** (added, +201/-0): Generated API Extractor report for the package-root entry point -- drift- and completeness-gated by the `api-docs` check.
- **docs/assets/og-image-source.svg** (added, +22/-0): Editable SVG source for the landing page's Open Graph image.
- **docs/assets/og-image.png** (added, +0/-0): Rasterized Open Graph preview image for the landing page (binary asset).
- **docs/favicon.png** (added, +0/-0): PNG favicon fallback for the landing page (binary asset).
- **docs/favicon.svg** (added, +5/-0): SVG favicon for the landing page.
- **docs/index.html** (added, +1595/-0): The GitHub Pages landing page -- no build step; inline theme bootstrap, and the target of the `accessibility` check's real headless-Chromium run.
- **docs/robots.txt** (added, +4/-0): Allows all crawlers and points to the sitemap.
- **docs/script.js** (added, +288/-0): Landing-page progressive-enhancement JS -- mobile nav toggle, theme switch, copy buttons.
- **docs/sitemap.xml** (added, +8/-0): Single-URL sitemap for the landing page.
- **docs/styles.css** (added, +1097/-0): Landing-page stylesheet -- plain CSS design tokens plus a small component layer, WCAG-contrast checked.
- **eslint-rules/require-stryker-rationale.d.mts** (added, +5/-0): Type declaration for the custom ESLint rule so `eslint.config.js` can import it typed.
- **eslint-rules/require-stryker-rationale.mjs** (added, +89/-0): Custom ESLint rule requiring a real explanatory comment adjacent to every `// Stryker disable` directive so mutation-kill exemptions stay auditable at the point they're introduced.
- **eslint.config.js** (added, +908/-0): The full flat ESLint config -- every plugin, the layering/boundary rules, the network-free-surface enforcement, the `security/*` no-suppress policy, and the custom Stryker-rationale rule.
- **knip.json** (added, +43/-0): Knip config for the `dead-code` check -- entry points, project globs, and the dependency-ignore list.
- **LICENSE** (modified, +1/-1): MIT license -- copyright holder updated from "Maverick Manasco" to "MaverickCER".
- **package-lock.json** (added, +18488/-0): npm lockfile pinning the full dependency tree.
- **package.json** (added, +186/-0): Package manifest -- dual ESM/CJS `exports` for `.` and `./presets`, `engines.node >=20`, the optional `yaml` peer dependency, and every verification/lifecycle script.
- **presets/package.json** (added, +5/-0): Redirect stub so `repo-contract/presets` resolves to `dist/presets.*` (main/module/types).
- **README.md** (added, +955/-0): The narrative walkthrough -- what repo-contract is, why contracts, quick start, the preset catalog, and the runtime support matrix.
- **repo-contract.config.ts** (added, +238/-0): repo-contract's own self-hosting contract -- wires every `checks/*` and preset check into a dependency graph in the required topological declaration order.
- **schemas/evidence.schema.json** (added, +253/-0): Published JSON Schema for the `Evidence` type -- generated from `src/types.ts`, independently versioned.
- **schemas/verdict.schema.json** (added, +66/-0): Published JSON Schema for the `Verdict` type -- generated from `src/types.ts`, independently versioned.
- **scripts/adr-governance/check.ts** (added, +117/-0): `adr-governance` evidence producer -- diffs governed paths and the ADR directory against the base ref, scans the changeset for valid `ADR NNNN` references, and prints JSON evidence to stdout.
- **scripts/adr-governance/evidence-types.ts** (added, +31/-0): Internal evidence contract for the adr-governance check -- `satisfied` is the single fact the policy gates on.
- **scripts/aggregate-coverage-paths.d.mts** (added, +2/-0): Type declaration for `aggregate-coverage-paths.mjs`.
- **scripts/aggregate-coverage-paths.mjs** (added, +7/-0): Single source of truth for the aggregate coverage artifact's path -- imported by both the writer (`aggregate-coverage.mjs`) and a reader (`checks/crap.ts`).
- **scripts/aggregate-coverage.d.mts** (added, +7/-0): Type declaration for `aggregate-coverage.mjs`.
- **scripts/aggregate-coverage.mjs** (added, +81/-0): Merges unit/integration/property coverage into a union `CoverageMap` (istanbul `merge`, never summed or averaged); never runs tests or enforces thresholds.
- **scripts/api-contract/baseline-store.ts** (added, +245/-0): The only module that touches `.repo-contract/api-contract/` -- reads the committed baseline from git HEAD (never the working tree) and is the single baseline writer.
- **scripts/api-contract/changeset-manager.ts** (added, +324/-0): Maintains the machine-owned "API Contract Impact" section of the shared changeset file -- parses frontmatter/marker level and reconciles the required release level.
- **scripts/api-contract/check.ts** (added, +310/-0): `api-contract` evidence producer -- runs API Extractor, normalizes baseline vs current, classifies compatibility, and prints only JSON evidence to stdout.
- **scripts/api-contract/compatibility-classifier.ts** (added, +588/-0): Pure diff over two normalized member maps -- implements every deterministic compatibility rule, delegating to `resolveAssignability` only when a type-bearing excerpt's text actually changed.
- **scripts/api-contract/evidence-types.ts** (added, +131/-0): Rich discriminated-union evidence model for the api-contract check -- verbose by design for compiler-enforced exhaustiveness as the classifier grows (ADR 0008).
- **scripts/api-contract/extractor-adapter.ts** (added, +147/-0): The only file importing `@microsoft/api-extractor` directly -- builds `IConfigFile` programmatically from repo conventions and isolates the rest of the feature from API Extractor internals.
- **scripts/api-contract/model-normalizer.ts** (added, +259/-0): Walks an `ApiPackage` into a flat, deterministic map keyed by canonical reference -- the feature's highest-risk file, tested against real installed API Extractor model classes.
- **scripts/api-contract/schema-version-consistency.ts** (added, +127/-0): Separate narrow pass -- flags an exported interface whose shape changed while its literal-typed `version` member didn't; the one otherwise-advisory finding that can fail the policy.
- **scripts/api-contract/semver.ts** (added, +93/-0): Hand-rolled `X.Y.Z` parsing/comparison -- the feature's single version-comparison implementation, with no `semver` dependency.
- **scripts/api-contract/summarize-changes.ts** (added, +52/-0): Builds `ApiContractEvidence.summary` deterministically from structured change data -- never hand-authored per scenario.
- **scripts/api-contract/type-assignability.ts** (added, +449/-0): The TypeChecker probe engine -- builds an in-memory `ts.Program` with virtual baseline/current declaration files plus a synthetic probe statement, then reads real `getSemanticDiagnostics()`; returns `"unknown"` rather than guessing.
- **scripts/api-contract/update-baseline.ts** (added, +93/-0): Human-invoked `npm run contract:baseline` -- the only normal way an existing baseline is updated; refuses unless `package.json`'s version is strictly greater than the baseline's.
- **scripts/api-docs/check.ts** (added, +80/-0): `api-docs` evidence producer -- regenerates every report target into a scratch directory and compares against the committed `docs/api-report/` files without mutating them.
- **scripts/api-docs/evidence-types.ts** (added, +25/-0): Internal evidence contract for the api-docs check -- per-target up-to-date flag and undocumented-marker list.
- **scripts/api-docs/generate.ts** (added, +30/-0): Human-invoked `npm run api-docs:generate` -- regenerates `docs/api-report/*.api.md` in place for review and commit.
- **scripts/api-docs/report-targets.ts** (added, +95/-0): Declares the two documented entry points and the shared extraction routine both `check.ts` and `generate.ts` run -- points at the real `.dts/` files, not the re-export shims.
- **scripts/architecture/evidence-types.ts** (added, +71/-0): Evidence contract for `check-architecture.mjs` -- the dependency-graph, test-boundary, and ADR-structure sections, each with a tool-infrastructure-failure `ok: false` variant.
- **scripts/changeset-docs/check.ts** (added, +56/-0): `changeset-docs` evidence producer -- lists changed files against the base ref, reconciles the per-file description table, and prints JSON evidence.
- **scripts/changeset-docs/evidence-types.ts** (added, +33/-0): Internal evidence contract for the changeset-docs check -- `allDescribed` is the single fact the policy gates on; rows are always sorted by path.
- **scripts/changeset-docs/table-manager.ts** (added, +208/-0): Reconciles the machine-owned "### Changed Files" section against the diff -- adds/keeps/drops rows and preserves each existing description verbatim, including across a detected rename.
- **scripts/changeset-file-locator.ts** (added, +72/-0): Shared logic picking the single `.changeset/*.md` file both machine-owned sections write into -- recognizes any `<!-- repo-contract:<owner>:start:` marker.
- **scripts/check-accessibility.mjs** (added, +47/-0): Entry point for the `accessibility` check -- runs pa11y against `docs/index.html` via a `file://` URL through its Node API (with `--no-sandbox` for Chromium on restricted CI kernels) and prints a `{ ok, value | error }` result.
- **scripts/check-adr-structure.d.mts** (added, +12/-0): Type declaration for `check-adr-structure.mjs`.
- **scripts/check-adr-structure.mjs** (added, +99/-0): Static ADR-set guardrail -- filename shape, no duplicate numbers, and the five required headings; evaluates the current tree only, numbering gaps allowed.
- **scripts/check-architecture.mjs** (added, +103/-0): Entry point for the `architecture` check -- runs dependency-cruiser plus the test-boundary and ADR-structure analyses and prints one combined JSON evidence object.
- **scripts/check-coverage.mjs** (added, +18/-0): Entry point for the `coverage` check -- aggregates the already-produced category coverage and prints the `total` summary as JSON; never runs tests.
- **scripts/check-docs.mjs** (added, +139/-0): Entry point for the `docs` check -- runs markdownlint-cli2 (JSON via file) and linkinator (`--markdown`) over the doc surface and prints combined JSON evidence.
- **scripts/check-lint.mjs** (added, +60/-0): Entry point for the `lint` check -- runs ESLint and oxlint (both `--fix`) and prints combined JSON evidence; report-only, so findings never fail the script itself.
- **scripts/check-size.mjs** (added, +120/-0): Dependency-free gzip-size reporter -- measures a fixed set of `dist/` entry points against a `--budget` but never judges the size itself.
- **scripts/check-test-boundaries.d.mts** (added, +14/-0): Type declaration for `check-test-boundaries.mjs`.
- **scripts/check-test-boundaries.mjs** (added, +131/-0): Static guardrail that every `*.test.ts` belongs to exactly one Vitest category and each category's config `include` only references its own directory.
- **scripts/contracts.ts** (added, +53/-0): Cross-script-family constants -- the changeset directory and both machine-owned sections' heading/marker sets, kept independently delimited so each mechanism rewrites only its own section.
- **scripts/coverage-thresholds.d.mts** (added, +6/-0): Type declaration for `coverage-thresholds.mjs`.
- **scripts/coverage-thresholds.mjs** (added, +12/-0): Single source of truth for the repo's coverage thresholds -- imported by both the CLI reporter and the `coverage` check policy.
- **scripts/diff-files.ts** (added, +160/-0): `RawDiffFile` producer -- zips `git diff --numstat` and `--name-status` (both `-M`) over `<base>...HEAD`; every git failure collapses to `undefined`.
- **scripts/emit-dts-shims.mjs** (added, +28/-0): Writes thin `.d.ts`/`.d.cts` re-export shims pointing `package.json#exports` at the real `dist/.dts/` declarations (tsup's own dts pipeline can't emit declaration maps).
- **scripts/generate-json-schema.d.mts** (added, +9/-0): Type declaration for `generate-json-schema.mjs`.
- **scripts/generate-json-schema.mjs** (added, +172/-0): Regenerates `schemas/*.schema.json` (plus the internal disable-comments schema) from their source types via ts-json-schema-generator; `TARGETS` is the single list.
- **scripts/helpers.ts** (added, +88/-0): Shared changeset helpers -- `stripGeneratedSection`, frontmatter splitting, and changeset-file rendering, used by both machine-owned-section managers.
- **scripts/lint-config.d.mts** (added, +1/-0): Type declaration for `lint-config.mjs`.
- **scripts/lint-config.mjs** (added, +15/-0): The repo's own `dead-code` exemptions -- CLI-only devDependencies Knip has no way to see are used.
- **scripts/npm-pack.d.mts** (added, +16/-0): Type declaration for `npm-pack.mjs`.
- **scripts/npm-pack.mjs** (added, +148/-0): Shared `npm pack` mechanics -- isolates the `--json` payload from npm's log noise and routes `npm.cmd` through cross-spawn on Windows.
- **scripts/report-coverage.mjs** (added, +60/-0): Reads the already-aggregated coverage summary and prints each metric against threshold (OK / BELOW THRESHOLD); never re-aggregates or gates.
- **scripts/run-attw-to-file.mjs** (added, +60/-0): Runs attw with stdout redirected to a real file (working around attw's pipe-buffer truncation bug) and packs the tarball itself with `--ignore-scripts`.
- **scripts/run-contract.mjs** (added, +92/-0): The script `npm run contract` invokes -- imports the built package's public API and runs it against this repo's own config; prints a color-free summary and sets `process.exitCode` (never calls `process.exit()`).
- **scripts/run-coverage.mjs** (added, +60/-0): Canonical `npm run test:coverage` -- runs the three coverage-producing categories with `--coverage`, then aggregates, then reports thresholds; fails only on a real test failure.
- **scripts/run-mutation.mjs** (added, +39/-0): Entry point for the `mutation` check -- deletes Stryker's incremental cache first so the check's evidence always reflects a full, fresh analysis.
- **scripts/run-test-category.mjs** (added, +47/-0): Canonical "run verification category X" -- the single `vitest run --config` implementation every category caller shares; passes flags through verbatim and writes nothing of its own to stdout.
- **scripts/schema-types.ts** (added, +16/-0): Non-exported target types for `generate-json-schema.mjs` -- grounds the generic `Evidence`/`Verdict` so ts-json-schema-generator can resolve them as root types.
- **scripts/security-network/evidence-types.ts** (added, +35/-0): Shapes shared between the network-surface scan and its policy -- the capability-kind union and per-finding location (ADR 0013).
- **scripts/security-network/network-surface.d.mts** (added, +13/-0): Type declaration for `network-surface.mjs`.
- **scripts/security-network/network-surface.mjs** (added, +216/-0): Single source of truth for the no-network-I/O threat model -- core modules, third-party packages, restricted named imports/globals, and preset-command allow/review lists; imported by both ESLint and the AST scan.
- **scripts/security-network/scan.ts** (added, +351/-0): `security-network` evidence producer -- an independent AST scan of `src/**/*.ts` for prohibited or unverifiable network capability, deliberately not relying on ESLint being configured correctly.
- **scripts/smoke-consumer.mjs** (added, +166/-0): Standalone published-artifact smoke test -- CI's `published-floor` job runs it with Node built-ins only (no devDependency, no `tsx`, no src/dist import) on the package's `engines.node` floor: installs the packed `.tgz` into a fresh project and checks its documented entry points under both `import` and `require`.
- **scripts/suppression-governance/canonicalize-comment.ts** (added, +57/-0): Reduces a suppression comment to its canonical semantic content (strips decoration, collapses multi-line block directives) so a cosmetic reformat isn't seen as a removed suppression plus a new one.
- **scripts/suppression-governance/check.ts** (added, +155/-0): `suppression-governance` evidence producer -- loads and validates `disable-comments.json`, discovers suppressions, synchronizes the registry, and prints JSON evidence; report-only.
- **scripts/suppression-governance/disable-comments.schema.json** (added, +104/-0): Internal (unpublished) JSON Schema for `disable-comments.json` -- generated from the `DisableCommentRegistry` type.
- **scripts/suppression-governance/discover-suppressions.ts** (added, +143/-0): Scans each source file's comment tokens via the TypeScript scanner (JSX variant chosen by extension) and runs each through the domain recognizers.
- **scripts/suppression-governance/evidence-types.ts** (added, +218/-0): Shapes across the suppression-governance execution and policy layers -- `SUPPRESSION_CATEGORIES`/`VERIFICATION_METHODS` are the single sources of truth their derived unions come from (ADR 0007).
- **scripts/suppression-governance/find-source-files.ts** (added, +134/-0): The check's own explicit whole-repo source walker -- its own excluded-directory decision (not inherited from ESLint/gitignore/jscpd), with symlink containment.
- **scripts/suppression-governance/policy-config.ts** (added, +152/-0): Single source of truth for the suppression-justification policy -- per-domain/rule requirements, imported by the governing check and by `checks/mutation.ts`.
- **scripts/suppression-governance/recognizers.ts** (added, +168/-0): One recognizer per suppression domain -- interprets a canonicalized comment as a directive and derives its mechanical `reason`; adding a domain is one entry here.
- **scripts/suppression-governance/registry.ts** (added, +333/-0): Reads/validates/writes `disable-comments.json` -- repo-relative POSIX path validation as defense-in-depth against a hand-edited registry escaping the repository root.
- **scripts/suppression-governance/resolve-policy.ts** (added, +189/-0): Pure `(domain, rule)` -> policy resolution with "stricter wins" merging -- shared so `checks/suppression-governance.ts` and `checks/mutation.ts` can't drift.
- **scripts/suppression-governance/synchronize.ts** (added, +185/-0): Reconciles discovered suppressions against existing records -- unchanged or unambiguously-moved preserves the hand-authored fields; a genuinely-gone suppression drops them.
- **SECURITY.md** (added, +127/-0): Security policy -- the network-free-surface guarantee, the no-secrets-in-errors contract, and the disclosure process.
- **specs/architecture.md** (added, +291/-0): The architecture spec -- the execution -> evidence -> policy phased pipeline, module layering, and why Evidence and Verdict are separate concepts.
- **specs/decisions/0001-execution-and-policy-are-a-strict-sequential-contract.md** (added, +53/-0): ADR 0001 -- execution and policy are a strict sequential contract: every check finishes and all evidence is assembled before any policy runs.
- **specs/decisions/0002-policyresult-is-a-structured-plain-json-contract.md** (added, +67/-0): ADR 0002 -- `PolicyResult` is a structured plain-JSON contract (`{ outcome, rationale }`), not a class or thrown value.
- **specs/decisions/0003-dependson-and-isolated-are-two-scheduling-primitives.md** (added, +143/-0): ADR 0003 -- `dependsOn` and `isolated` are two distinct scheduling primitives (targeted edge vs. full barrier at declaration position).
- **specs/decisions/0004-cross-platform-command-execution-and-process-cleanup.md** (added, +63/-0): ADR 0004 -- cross-platform command execution and process-tree cleanup (POSIX process groups vs. Windows `taskkill /t`).
- **specs/decisions/0005-public-surface-stays-narrow-no-cli-experimental-presets.md** (added, +70/-0): ADR 0005 -- the public surface stays narrow: no CLI, and presets stay an opt-in extra separate from the core.
- **specs/decisions/0006-independent-verification-boundaries-coverage-is-a-union.md** (added, +65/-0): ADR 0006 -- independent verification boundaries; aggregate coverage is a union of covered locations, never a summed or averaged percentage.
- **specs/decisions/0007-suppression-governance.md** (added, +79/-0): ADR 0007 -- suppression governance: every lint/Stryker suppression carries an auditable, hand-authored justification in `disable-comments.json`.
- **specs/decisions/0008-api-contract-compatibility-gate.md** (added, +60/-0): ADR 0008 -- the API-contract compatibility gate: a structured diff of the public TypeScript surface against a committed baseline.
- **specs/decisions/0009-self-hosting-tool-and-dependency-choices.md** (added, +91/-0): ADR 0009 -- the self-hosting tool and dependency choices (which tools this repo runs on itself, and why each was picked).
- **specs/decisions/0010-changeset-adr-and-pr-documentation-discipline.md** (added, +76/-0): ADR 0010 -- changeset, ADR, and PR documentation discipline: three separate process/traceability mechanisms, each a filled-in scaffold rather than derived metadata.
- **specs/decisions/0011-bun-and-deno-runtime-support.md** (added, +78/-0): ADR 0011 -- Bun and Deno runtime support, and what "tested" covers for each.
- **specs/decisions/0012-contract-orchestration-races-are-fixed-with-dependson-not-retries.md** (added, +94/-0): ADR 0012 -- contract-orchestration races are fixed with `dependsOn`, not retries.
- **specs/decisions/0013-no-network-surface.md** (added, +139/-0): ADR 0013 -- no network surface: `src/` never performs network I/O, enforced by ESLint and an independent AST scan.
- **specs/verification-taxonomy.md** (added, +491/-0): The verification-category taxonomy -- what unit/integration/property/e2e/mutation/architecture each mean, and the coverage-contribution matrix.
- **src/config/define-repo-contract.ts** (added, +38/-0): `defineRepoContract` -- an identity function for type inference (output -> policy flow, static `dependsOn` validation); performs no runtime validation or cloning.
- **src/config/tokenize-command.ts** (added, +182/-0): POSIX-style `run`-string tokenizer -- splits a command into argv without a shell; no shell operator is executed and no glob is expanded.
- **src/config/validate-config.ts** (added, +346/-0): Structural `RepoContractConfig` validation that throws before any process spawns -- defensive about runtime field types, with dependency-cycle detection.
- **src/errors.ts** (added, +229/-0): The error hierarchy -- every error carries a stable `code` and never embeds stdout/stderr/env; three deliberately-separate failure classes (structural, execution-discovered, policy bug).
- **src/evidence/build-evidence.ts** (added, +91/-0): Attaches parsed output to each check's raw evidence and assembles the versioned, immutable `Evidence` object -- everything is fully assembled before any policy reads it.
- **src/execution/abort-signals.ts** (added, +56/-0): `composeSignals` -- combines signals into one that aborts on any input; native `AbortSignal.any` where available, a manual fallback for Node 20.0-20.2, with `dispose` to avoid listener leaks.
- **src/execution/concurrency-pool.ts** (added, +64/-0): `runWithConcurrency` -- a pure bounded-parallelism primitive preserving result order; the first worker rejection is rethrown after every sibling settles.
- **src/execution/dependency-scheduler.ts** (added, +216/-0): `runWithConcurrencyGraph` -- bounded parallelism plus a reactive dependency graph (incremental Kahn); a completing item immediately unblocks its dependents, with no wave boundary.
- **src/execution/process-tree.ts** (added, +105/-0): `shouldSpawnDetached` and `killTree` -- POSIX process-group kill vs. Windows `taskkill /t`, terminating a whole process tree rather than just the wrapper.
- **src/execution/run-checks.ts** (added, +348/-0): The run phase -- schedules every check, installs SIGINT/SIGTERM handlers that kill active check trees before self-terminating, and resolves `dependsOn`.
- **src/execution/spawn-check.ts** (added, +494/-0): Spawns one check's command -- 10 MiB per-stream output cap, timeout, abort, SIGTERM -> SIGKILL escalation, and structured `CheckEvidence`.
- **src/index.ts** (added, +44/-0): The public API barrel -- `defineRepoContract`, `runRepoContract`, the error classes, and the consumer-facing types; nothing internal is re-exported.
- **src/parsing/parse-json.ts** (added, +18/-0): Parses stdout as JSON -- a malformed-JSON failure is returned as data with raw stdout preserved, never thrown.
- **src/parsing/parse-output.ts** (added, +26/-0): Dispatches to the parser for the requested `format`; may throw `ParserDependencyMissingError` only for a missing `yaml` dependency.
- **src/parsing/parse-text.ts** (added, +10/-0): A trimmed-passthrough "parse" that always succeeds -- the self-documenting alternative to omitting `output` entirely.
- **src/parsing/parse-yaml.ts** (added, +40/-0): Parses stdout as YAML via a lazy `import("yaml")` -- a missing dependency throws `ParserDependencyMissingError`; malformed YAML is a normal data failure.
- **src/policy/run-policies.ts** (added, +253/-0): The policy phase -- runs every check's policy against the full evidence, aggregates the verdict, and turns a policy that reads unrequested or unparsed output into a typed error.
- **src/presets/arethetypeswrong.ts** (added, +100/-0): `arethetypeswrong` preset -- interprets attw's `--format json` problems into one human-readable line each.
- **src/presets/broken-links.ts** (added, +85/-0): `brokenLinks` preset -- linkinator over local files and their targets; the `start` target is passed straight through.
- **src/presets/commitlint.ts** (added, +50/-0): `commitlint` preset -- exit-code-based commit-message governance over a `from..to` range, using the consumer's own commitlint config.
- **src/presets/dead-code.ts** (added, +185/-0): `deadCode` preset -- models Knip's JSON reporter contract and reports the actual issue category and name, not just a count.
- **src/presets/duplication.ts** (added, +116/-0): `duplication` preset -- jscpd over a `path`, reading its JSON report from disk.
- **src/presets/e2e.ts** (added, +132/-0): `e2e` preset -- recursively collects failing specs from Playwright's `--reporter=json` suite tree.
- **src/presets/format.ts** (added, +25/-0): `format` preset -- `prettier --write .`, exit-code based.
- **src/presets/index.ts** (added, +30/-0): The `repo-contract/presets` barrel -- a curated catalog, never re-exported from the package root.
- **src/presets/license.ts** (added, +86/-0): `license` preset -- licensee over the production dependency graph (`--osi --errors-only --ndjson`); an empty stdout from a non-zero exit is a genuine failure, not a pass.
- **src/presets/lint.ts** (added, +99/-0): `lint` preset -- ESLint's JSON formatter: errors fail, warnings warn; `path` option scopes the target.
- **src/presets/markdownlint.ts** (added, +105/-0): `markdownlint` preset -- markdownlint-cli2's JSON report read from disk; `glob` option scopes the target.
- **src/presets/publint.ts** (added, +38/-0): `publint` preset -- reads publint's own "Errors:/Warnings:/Suggestions:" section headers to separate blocking from non-blocking findings.
- **src/presets/security-deps.ts** (added, +134/-0): `securityDeps` preset -- `npm audit --omit=dev --json`; the blocking severity threshold is repository policy, and there is no missing-dependency guard (it shells out to npm itself).
- **src/presets/security-secrets.ts** (added, +81/-0): `securitySecrets` preset -- secretlint's JSON report; secret values are deliberately excluded from the policy output.
- **src/presets/shared/error-warning-pass-policy.ts** (added, +39/-0): Shared fail/warn/pass shape for the lint-style presets (ESLint, stylelint) -- only the tool name substituted into each rationale differs.
- **src/presets/shared/exit-code-fail-rationale.ts** (added, +34/-0): `combinedOutput` and `exitCodeFailRationale` -- the shared "here's what the tool printed" renderer for the exit-code presets (`format`, `typecheck`, `commitlint`).
- **src/presets/shared/missing-dependency.ts** (added, +31/-0): `checkDependencyInstalled` -- turns an `ENOENT` spawn error into an actionable "install X" `PolicyResult`; other spawn errors fall through unchanged.
- **src/presets/shared/read-json-report.ts** (added, +46/-0): `readJsonReport` -- read-then-parse a tool's on-disk JSON report with separate caller-supplied rationales; takes the read as a thunk so the path literal stays at the call site.
- **src/presets/shared/terminal-status.ts** (added, +64/-0): `checkTerminatedAbnormally` -- reports the real terminal cause (timeout / abort / host-terminated / signaled / spawn error) instead of a fabricated tool verdict on a `null` exit code.
- **src/presets/shared/vitest-json-policy.ts** (added, +86/-0): `evaluateVitestJsonPolicy` -- interprets Vitest's `--reporter=json` shape only; valid-but-wrong-shape JSON fails rather than throwing out of the policy.
- **src/presets/stylelint.ts** (added, +85/-0): `stylelint` preset -- stylelint's `--formatter json`: `error` blocks, `warning` doesn't; `glob` option scopes the target.
- **src/presets/test.ts** (added, +19/-0): `test` preset -- a single, un-categorized `vitest run --reporter=json`.
- **src/presets/typecheck.ts** (added, +25/-0): `typecheck` preset -- `tsc --noEmit -p tsconfig.json`, exit-code based.
- **src/run-repo-contract.ts** (added, +80/-0): `runRepoContract` -- validate -> execute -> build evidence -> evaluate policies -> aggregate verdict; strictly phased, and never calls `process.exit()`.
- **src/types.ts** (added, +340/-0): The public type surface -- `Evidence`, `Verdict`, `CheckStatus`, `PolicyResult`, `RepoContractConfig`, and friends; Evidence describes what happened, Verdict whether it was acceptable.
- **stryker.config.mjs** (added, +38/-0): Stryker config -- Vitest runner, `coverageAnalysis: "perTest"` with `ignoreStatic` for a CI-viable run time, the `mutate` globs (the process/signal/scheduling core is excluded -- mutating it destabilizes the Stryker runner itself; covered by the integration/e2e categories instead), and the JSON reporter `checks/mutation.ts` reads.
- **SUPPORT.md** (added, +50/-0): Where to ask questions and file issues.
- **test/e2e/consumer-install-bun.test.ts** (added, +137/-0): E2E -- installs the real packed tarball into a fresh project and runs its entry points under Bun (ADR 0011).
- **test/e2e/consumer-install-deno.test.ts** (added, +153/-0): E2E -- the same packed-tarball install exercised under Deno with `--allow-read/-run/-env` (ADR 0011).
- **test/e2e/consumer-install.test.ts** (added, +204/-0): E2E -- packed-tarball install under Node, verifying the `exports` map and `dist/` contents work under both `import` and `require`.
- **test/helpers/api-contract/build-fixture-package.ts** (added, +135/-0): Test helper -- compiles a tiny real TS file and runs the real API Extractor adapter to produce a genuine `ApiPackage`/`.d.ts` rollup for the api-contract tests.
- **test/helpers/pack-consumer.ts** (added, +85/-0): Test helper -- the shared pack/install/cleanup mechanics for the consumer-install E2E suites (Node/Bun/Deno).
- **test/integration/adr-governance/check.integration.test.ts** (added, +132/-0): Integration -- the real adr-governance check path (git diff -> reference resolution -> evidence) against a scratch fixture repository.
- **test/integration/api-contract/check.integration.test.ts** (added, +259/-0): Integration -- the full api-contract check path against real compiled fixture packages and `.d.ts` rollups.
- **test/integration/changeset-docs/check.integration.test.ts** (added, +168/-0): Integration -- the real changeset-docs check path (git diff -> table reconciliation -> evidence) against a scratch fixture repository.
- **test/integration/lint/no-concurrent-fixers.test.ts** (added, +96/-0): Integration -- guards that no two configured fixers (ESLint, oxlint, Prettier) would fight over the same files.
- **test/integration/run-repo-contract.test.ts** (added, +477/-0): Integration -- `runRepoContract` end to end: zero checks, a single check, dependency ordering, policy-threw propagation, and spawn errors.
- **test/integration/suppression-governance/check.integration.test.ts** (added, +215/-0): Integration -- the real suppression-governance check (discover -> synchronize -> evidence) against fixture source and registry.
- **test/integration/suppression-governance/real-source.integration.test.ts** (added, +63/-0): Integration -- runs suppression discovery against this repository's own real source tree.
- **test/property/changeset-file-locator.property.test.ts** (added, +77/-0): Property -- `locateTargetFileName` invariants over generated `.changeset/` directory states.
- **test/property/changeset-frontmatter.property.test.ts** (added, +80/-0): Property -- changeset frontmatter parse/render round-trips over generated inputs.
- **test/property/changeset-manager-level.property.test.ts** (added, +89/-0): Property -- the api-contract changeset-manager's release-level reconciliation over generated human/machine level pairs.
- **test/property/compatibility-classifier.property.test.ts** (added, +134/-0): Property -- compatibility-classifier invariants (e.g. impact never silently downgraded) over generated normalized-map diffs.
- **test/property/concurrency-pool.property.test.ts** (added, +42/-0): Property -- `runWithConcurrency` preserves order and respects the bound over generated item lists and concurrency values.
- **test/property/dependency-scheduler.property.test.ts** (added, +96/-0): Property -- `runWithConcurrencyGraph` respects dependency ordering and the concurrency bound over generated acyclic graphs.
- **test/property/diff-files-parsing.property.test.ts** (added, +82/-0): Property -- `diff-files` numstat/name-status zipping over generated git-diff output pairs.
- **test/property/parse-output.property.test.ts** (added, +79/-0): Property -- parser dispatch never throws for malformed input over generated stdout.
- **test/property/schema-version-consistency.property.test.ts** (added, +123/-0): Property -- the schema-version-literal pass over generated interface-shape changes.
- **test/property/semver.property.test.ts** (added, +80/-0): Property -- `compareVersions` reflexivity/antisymmetry and the other semver-helper laws.
- **test/property/spawn-check.status.property.test.ts** (added, +56/-0): Property -- `spawnCheck` maps every terminal condition to the correct `CheckStatus` over generated exit/signal combinations.
- **test/property/summarize-changes.property.test.ts** (added, +101/-0): Property -- `summarizeChanges` output is deterministic and stable-ordered over generated change lists.
- **test/property/table-manager-row.property.test.ts** (added, +83/-0): Property -- the changeset-docs row render/parse round-trip preserves descriptions over generated file/description inputs.
- **test/property/tokenize-command.property.test.ts** (added, +99/-0): Property -- `tokenizeCommand` quoting/escaping invariants over generated command strings.
- **test/property/validate-dependency-graph.property.test.ts** (added, +87/-0): Property -- config cycle detection over generated dependency graphs (cyclic always rejected, acyclic always accepted).
- **test/unit/adr-governance/policy.test.ts** (added, +82/-0): Unit -- `checks/adr-governance.ts` policy over each classified determinant.
- **test/unit/api-contract/baseline-store.test.ts** (added, +200/-0): Unit -- `baseline-store` reads from git HEAD, ignores working-tree edits, and behaves as a single writer.
- **test/unit/api-contract/changeset-manager.test.ts** (added, +523/-0): Unit -- the "API Contract Impact" section manager: frontmatter/marker parsing, level reconciliation, and section rewrite.
- **test/unit/api-contract/compatibility-classifier.test.ts** (added, +436/-0): Unit -- every deterministic compatibility rule exercised with a fake `resolveAssignability`.
- **test/unit/api-contract/model-normalizer.test.ts** (added, +214/-0): Unit -- normalizes real API Extractor model fixtures into the deterministic member map.
- **test/unit/api-contract/policy.test.ts** (added, +214/-0): Unit -- `checks/api-contract.ts` policy over each impact / changeset-action combination.
- **test/unit/api-contract/schema-version-consistency.test.ts** (added, +111/-0): Unit -- the version-literal defect pass against constructed interface changes.
- **test/unit/api-contract/semver.test.ts** (added, +71/-0): Unit -- `parseVersion`/`compareVersions`/`computeMinimumRequiredVersion` edge cases.
- **test/unit/api-contract/summarize-changes.test.ts** (added, +70/-0): Unit -- `summarizeChanges` fixed sentences and list rendering.
- **test/unit/api-contract/type-assignability.test.ts** (added, +286/-0): Unit -- the real-TypeChecker assignability probe over covariant/contravariant/invariant positions, including the `"unknown"` fallback.
- **test/unit/api-contract/update-baseline.test.ts** (added, +121/-0): Unit -- `runUpdateBaseline` guardrails (refuses unless the version is strictly greater; reports API Extractor errors).
- **test/unit/api-docs/check.test.ts** (added, +38/-0): Unit -- the api-docs drift and undocumented-marker detection against real repository files.
- **test/unit/api-docs/policy.test.ts** (added, +63/-0): Unit -- `checks/api-docs.ts` policy over out-of-date reports and undocumented markers.
- **test/unit/architecture/check-adr-structure.test.ts** (added, +122/-0): Unit -- ADR-structure validation (filename shape, duplicate numbers, required headings, accepted numbering gaps).
- **test/unit/architecture/check-test-boundaries.test.ts** (added, +93/-0): Unit -- the test-category boundary checks (one category per file, config `include` scoping).
- **test/unit/architecture/fixtures/clean/evidence/build.ts** (added, +10/-0): Fixture -- a legitimate `evidence/ -> execution/` import the layering rules permit (the true-negative case for `rules.test.ts`).
- **test/unit/architecture/fixtures/clean/execution/run.ts** (added, +3/-0): Fixture -- the clean `execution/` leaf module imported by the clean `build.ts` fixture.
- **test/unit/architecture/fixtures/violating/execution/spawn.ts** (added, +10/-0): Fixture -- a forbidden `execution/ -> policy/` import (ADR 0001), the true-positive case for the layering rule.
- **test/unit/architecture/fixtures/violating/policy/evaluate.ts** (added, +3/-0): Fixture -- the `policy/` leaf module imported by the violating `spawn.ts` fixture.
- **test/unit/architecture/policy.test.ts** (added, +161/-0): Unit -- `checks/architecture.ts` policy: fails only on `error`-severity violations across all three sections.
- **test/unit/architecture/rules.test.ts** (added, +81/-0): Unit -- runs the real `.dependency-cruiser.cjs` rules against the clean and violating fixtures (true positive and true negative).
- **test/unit/changeset-docs/policy.test.ts** (added, +67/-0): Unit -- `checks/changeset-docs.ts` policy over each determinant (nothing to document / all described / undescribed).
- **test/unit/changeset-docs/table-manager.test.ts** (added, +240/-0): Unit -- table reconciliation: add/keep/drop rows, description preservation across renames, and marker handling.
- **test/unit/changeset-file-locator.test.ts** (added, +82/-0): Unit -- target-file selection across dedicated-name, existing-marker, and multi-file `.changeset/` states.
- **test/unit/checks/shared/require-parsed-output.test.ts** (added, +32/-0): Unit -- `requireParsedOutput` returns the fail result on missing/failed output and the narrowed value otherwise.
- **test/unit/config/define-repo-contract.test.ts** (added, +166/-0): Unit -- `defineRepoContract` returns the config untouched, plus its type-level inference behavior.
- **test/unit/config/tokenize-command.test.ts** (added, +181/-0): Unit -- command tokenization: quotes, escapes, Windows paths in single quotes, and that no shell operator is executed.
- **test/unit/config/validate-config.test.ts** (added, +527/-0): Unit -- every structural config rejection, the valid zero-checks case, and dependency-cycle detection.
- **test/unit/coverage/aggregate-coverage-paths.test.ts** (added, +48/-0): Unit -- the aggregate coverage path constants stay consistent.
- **test/unit/coverage/policy.test.ts** (added, +59/-0): Unit -- `checks/coverage.ts` per-metric threshold comparison, including missing/invalid percentages.
- **test/unit/crap/policy.test.ts** (added, +88/-0): Unit -- `checks/crap.ts` threshold comparison and invalid-report handling.
- **test/unit/cross-platform/windows-taskkill.test.ts** (added, +59/-0): Unit -- `killTree` issues `taskkill /t` on win32 and a process-group kill on POSIX.
- **test/unit/diff-files.test.ts** (added, +125/-0): Unit -- `diff-files` status/rename detection and numstat zipping; a git failure yields `undefined`.
- **test/unit/docs/policy.test.ts** (added, +130/-0): Unit -- `checks/docs.ts` policy over markdownlint errors and broken links.
- **test/unit/errors.test.ts** (added, +235/-0): Unit -- every error class's stable `code`, and that messages never embed stdout/stderr/env.
- **test/unit/evidence/build-evidence-output-parse-failure-aggregation.test.ts** (added, +53/-0): Unit -- a parse failure is attached as data and aggregated, never thrown.
- **test/unit/evidence/build-evidence-yaml-missing-dependency.test.ts** (added, +43/-0): Unit -- a missing `yaml` dependency surfaces as `ParserDependencyMissingError` during evidence build.
- **test/unit/evidence/build-evidence.test.ts** (added, +141/-0): Unit -- assembles the versioned, immutable `Evidence` and the flat parsed-entry array.
- **test/unit/execution/abort-signals.test.ts** (added, +127/-0): Unit -- `composeSignals` native and fallback paths, abort-reason propagation, and `dispose` listener cleanup.
- **test/unit/execution/concurrency-pool.test.ts** (added, +118/-0): Unit -- order preservation, the concurrency bound, and the deferred first-rejection rethrow.
- **test/unit/execution/dependency-scheduler.test.ts** (added, +308/-0): Unit -- dependency ordering, diamond/fan-out shapes, the stall guard, and worker-rejection forwarding.
- **test/unit/execution/fixtures/sigint-cleanup-queued.ts** (added, +43/-0): Fixture -- a real child process with a queued (not-yet-started) check, for the SIGINT-cleanup test.
- **test/unit/execution/fixtures/sigint-cleanup.ts** (added, +36/-0): Fixture -- a real child process running a check, spawned via tsx so a real SIGINT can be sent from the test.
- **test/unit/execution/process-tree.test.ts** (added, +174/-0): Unit -- `shouldSpawnDetached` per platform and `killTree` tree-termination behavior.
- **test/unit/execution/run-checks.test.ts** (added, +623/-0): Unit -- the run phase: scheduling, `dependsOn` resolution, host SIGINT/SIGTERM cleanup, and self-terminate timing.
- **test/unit/execution/spawn-check-escalation-guards.test.ts** (added, +89/-0): Unit -- the SIGTERM -> SIGKILL escalation timers and their guards.
- **test/unit/execution/spawn-check.test.ts** (added, +553/-0): Unit -- `spawnCheck`: output cap, timeout, abort, invalid config, `activeHandles` mid-flight, and status mapping.
- **test/unit/index.test.ts** (added, +52/-0): Unit -- the public barrel re-exports exactly the intended surface and nothing internal.
- **test/unit/lint/policy.test.ts** (added, +145/-0): Unit -- `checks/lint.ts` policy over combined ESLint/oxlint evidence (errors fail, warnings warn, infra failure fails).
- **test/unit/lint/require-stryker-rationale.test.ts** (added, +80/-0): Unit -- the custom ESLint rule flags an unexplained `// Stryker disable` and accepts a real adjacent rationale.
- **test/unit/mutation/policy.test.ts** (added, +408/-0): Unit -- `checks/mutation.ts` policy: every mutant status, the comment-ignored cross-check against `disable-comments.json`, and invalid-report handling.
- **test/unit/npm-pack.test.ts** (added, +36/-0): Unit -- `parseNpmPackFilename` isolates the JSON block from npm's log noise and asserts its shape.
- **test/unit/parsing/parse-json.test.ts** (added, +0/-0): Unit -- `parseJson` over valid JSON, arrays, primitives, and malformed input (failure returned as data with raw stdout preserved).
- **test/unit/parsing/parse-output.test.ts** (added, +24/-0): Unit -- `parseOutput` dispatches to the correct parser per format.
- **test/unit/parsing/parse-text.test.ts** (added, +20/-0): Unit -- `parseText` trims and always succeeds.
- **test/unit/parsing/parse-yaml-missing-dependency.test.ts** (added, +31/-0): Unit -- `parseYaml` throws `ParserDependencyMissingError` when `yaml` can't be imported.
- **test/unit/parsing/parse-yaml.test.ts** (added, +33/-0): Unit -- `parseYaml` parses valid YAML and returns malformed YAML as a data failure.
- **test/unit/policy/run-policies.test.ts** (added, +1057/-0): Unit -- the policy phase: verdict aggregation, `PolicyThrewError`/`AggregateError`, and reading unrequested/unparsed output as typed errors.
- **test/unit/presets/arethetypeswrong.test.ts** (added, +122/-0): Unit -- the `arethetypeswrong` preset over attw JSON problems and the terminal/missing-dependency cases.
- **test/unit/presets/broken-links.test.ts** (added, +112/-0): Unit -- the `brokenLinks` preset over linkinator reports.
- **test/unit/presets/commitlint.test.ts** (added, +52/-0): Unit -- the `commitlint` preset's exit-code interpretation and missing-dependency guard.
- **test/unit/presets/dead-code.test.ts** (added, +304/-0): Unit -- the `deadCode` preset over each Knip issue category.
- **test/unit/presets/duplication.test.ts** (added, +158/-0): Unit -- the `duplication` preset over jscpd reports and read/parse failures.
- **test/unit/presets/e2e.test.ts** (added, +297/-0): Unit -- the `e2e` preset's recursive failing-spec collection from Playwright JSON.
- **test/unit/presets/fixtures.ts** (added, +42/-0): Test helper -- `fakeCheckEvidence`/`fakeContext` builders shared by the preset unit tests.
- **test/unit/presets/format.test.ts** (added, +41/-0): Unit -- the `format` preset's exit-code interpretation and guards.
- **test/unit/presets/index.test.ts** (added, +133/-0): Unit -- the presets barrel exports every documented preset and nothing else.
- **test/unit/presets/license.test.ts** (added, +88/-0): Unit -- the `license` preset: the offending-dependency ndjson path vs. the empty-stdout / non-zero-exit failure path.
- **test/unit/presets/lint.test.ts** (added, +126/-0): Unit -- the `lint` preset over ESLint JSON (errors / warnings / clean) and guards.
- **test/unit/presets/markdownlint.test.ts** (added, +117/-0): Unit -- the `markdownlint` preset over its on-disk JSON report.
- **test/unit/presets/publint.test.ts** (added, +61/-0): Unit -- the `publint` preset's section-header parsing (Errors / Warnings / Suggestions).
- **test/unit/presets/security-deps.test.ts** (added, +157/-0): Unit -- the `securityDeps` preset over `npm audit` JSON at each severity.
- **test/unit/presets/security-secrets.test.ts** (added, +117/-0): Unit -- the `securitySecrets` preset over secretlint JSON, with secret values excluded from output.
- **test/unit/presets/shared/missing-dependency.test.ts** (added, +61/-0): Unit -- `checkDependencyInstalled` on `ENOENT` vs. other spawn errors.
- **test/unit/presets/shared/terminal-status.test.ts** (added, +59/-0): Unit -- `checkTerminatedAbnormally` for each non-`completed` status.
- **test/unit/presets/shared/vitest-json-policy.test.ts** (added, +225/-0): Unit -- `evaluateVitestJsonPolicy` over Vitest JSON shapes, including valid-but-wrong-shape -> fail.
- **test/unit/presets/stylelint.test.ts** (added, +171/-0): Unit -- the `stylelint` preset over its JSON formatter output (errors block, warnings don't).
- **test/unit/presets/test.test.ts** (added, +40/-0): Unit -- the `test` preset delegating to the Vitest JSON policy, plus guards.
- **test/unit/presets/typecheck.test.ts** (added, +38/-0): Unit -- the `typecheck` preset's exit-code interpretation and guards.
- **test/unit/schema/schema-conformance.test.ts** (added, +191/-0): Unit -- the regenerated `schemas/*.schema.json` match their source types and carry the declared `$id`/`title`.
- **test/unit/security-network/fixtures/clean/src/example-preset.ts** (added, +8/-0): Fixture -- a clean `src/` module with no network capability (the true-negative case for the scan).
- **test/unit/security-network/fixtures/violating/src/direct-network-import.ts** (added, +8/-0): Fixture -- a direct `node:http` import (the simplest scan violation).
- **test/unit/security-network/fixtures/violating/src/dynamic-bypass.ts** (added, +9/-0): Fixture -- a dynamic `import()` with a non-literal specifier (an unverifiable capability).
- **test/unit/security-network/fixtures/violating/src/malicious-preset.ts** (added, +10/-0): Fixture -- a preset returning a non-literal / unreviewed command (prohibited by the scan).
- **test/unit/security-network/network-surface.test.ts** (added, +38/-0): Unit -- the shared network-surface data module's shape and contents.
- **test/unit/security-network/policy.test.ts** (added, +48/-0): Unit -- `checks/security-network.ts` policy over clean and finding-bearing evidence.
- **test/unit/security-network/scan.test.ts** (added, +277/-0): Unit -- the AST scan over the clean and violating fixtures, one case per capability kind.
- **test/unit/size/policy.test.ts** (added, +60/-0): Unit -- `checks/size.ts` policy matching measured gzip figures against the budgets by file.
- **test/unit/suppression-governance/canonicalize-comment.test.ts** (added, +49/-0): Unit -- comment canonicalization (decoration stripping, multi-line block collapse, one-line/multi-line equivalence).
- **test/unit/suppression-governance/check.test.ts** (added, +60/-0): Unit -- the check's registry load/validate path and tool-infrastructure-failure handling.
- **test/unit/suppression-governance/discover-suppressions.test.ts** (added, +198/-0): Unit -- comment-token discovery across TS/JSX variants and recognizer dispatch.
- **test/unit/suppression-governance/find-source-files.test.ts** (added, +132/-0): Unit -- the source walker's excluded-directory decisions and symlink containment.
- **test/unit/suppression-governance/policy.test.ts** (added, +440/-0): Unit -- `checks/suppression-governance.ts` policy: `suppressionPolicy` validation and forbidden/under-justified suppression detection.
- **test/unit/suppression-governance/recognizers.test.ts** (added, +203/-0): Unit -- each domain recognizer's rule/reason parsing (including ESLint's `--` justification separator).
- **test/unit/suppression-governance/registry.test.ts** (added, +297/-0): Unit -- registry read/validate/write, including rejection of malformed and root-escaping paths.
- **test/unit/suppression-governance/synchronize.test.ts** (added, +235/-0): Unit -- synchronization: unchanged/moved preserves the hand-authored fields, a gone suppression drops them.
- **tsconfig.build.json** (added, +13/-0): Build tsconfig -- emits real declarations (with working maps) into `dist/.dts/` that tsup can't produce.
- **tsconfig.json** (added, +32/-0): Root tsconfig -- the strict settings (`noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`, and friends) shared across the toolchain.
- **tsup.config.ts** (added, +36/-0): tsup build config -- ESM + CJS bundles for `.` and `./presets`, `minifyWhitespace` only, `dts: false`.
- **VERSIONING.md** (added, +110/-0): The versioning policy -- pre-1.0 semantics, and the independent schema-version literals for `Evidence` and `Verdict`.
- **vitest.base.config.ts** (added, +50/-0): Shared Vitest settings across every category config; the fork count is capped at half the machine's core count.
- **vitest.config.ts** (added, +41/-0): Dev-convenience aggregate (unit + integration + property) for `npm run test:watch` and Stryker's default target -- not a verification category of its own.
- **vitest.e2e.config.ts** (added, +22/-0): The `e2e` category config -- `test/e2e/**` only.
- **vitest.integration.config.ts** (added, +24/-0): The `integration` category config -- `test/integration/**` only.
- **vitest.property.config.ts** (added, +24/-0): The `property` category config -- `test/property/**` only.
- **vitest.unit.config.ts** (added, +43/-0): The `unit` category config -- `test/unit/**` only; no per-category coverage thresholds.

<!-- repo-contract:changeset-docs:end -->

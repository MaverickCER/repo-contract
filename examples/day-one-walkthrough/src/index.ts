// A real project would have real code here. This walkthrough is about how a shared requirement
// rolls out across many such projects, so the code is deliberately trivial -- see ../README.md.
export function greet(name: string): string {
  return `Hello, ${name}.`
}

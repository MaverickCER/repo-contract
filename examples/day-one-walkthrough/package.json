{
  "name": "day-one-walkthrough",
  "version": "0.0.0",
  "private": true,
  "description": "Example: rolling out a new shared requirement across every repository at once, as a dated warn -> fail. See ./README.md.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "walkthrough": "tsx run.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@types/node": "^20.19.43",
    "repo-contract": "file:../..",
    "tsx": "^4.23.12",
    "typescript": "^5.9.3"
  }
}

{
  "exceptions": [
    {
      "id": "dead-code:unused-dev-dependency:@arethetypeswrong/cli",
      "version": 1,
      "justification": "Spawned by literal command name (\"attw\") via the arethetypeswrong preset's `run: [\"attw\", ...]` (src/presets/arethetypeswrong.ts) -- nothing `import`s the package, so knip cannot see the use.",
      "kind": "unused-dev-dependency",
      "name": "@arethetypeswrong/cli"
    },
    {
      "id": "dead-code:unused-dev-dependency:@commitlint/cli",
      "version": 1,
      "justification": "Spawned by literal command name (\"commitlint\") via the commitlint preset's `run: [\"commitlint\", ...]` (src/presets/commitlint.ts) and by .githooks/commit-msg -- nothing `import`s the package, so knip cannot see the use.",
      "kind": "unused-dev-dependency",
      "name": "@commitlint/cli"
    },
    {
      "id": "dead-code:unused-dev-dependency:@socketsecurity/cli",
      "version": 1,
      "justification": "Spawned by literal command name (\"socket\") via `cross-spawn` in scripts/security-socket/scan.ts -- nothing `import`s the package, so knip cannot see a spawned binary name as a use of the package that provides it.",
      "kind": "unused-dev-dependency",
      "name": "@socketsecurity/cli"
    },
    {
      "id": "dead-code:unused-dev-dependency:licensee",
      "version": 1,
      "justification": "Spawned by literal command name via the license preset's `run: [\"licensee\", ...]` (src/presets/license.ts) -- nothing `import`s the package, so knip cannot see the use.",
      "kind": "unused-dev-dependency",
      "name": "licensee"
    },
    {
      "id": "dead-code:unused-dev-dependency:linkinator",
      "version": 1,
      "justification": "Spawned by literal command name via the broken-links preset's `run: [\"linkinator\", ...]` (src/presets/broken-links.ts) -- nothing `import`s the package, so knip cannot see the use.",
      "kind": "unused-dev-dependency",
      "name": "linkinator"
    },
    {
      "id": "dead-code:unused-dev-dependency:oxlint",
      "version": 1,
      "justification": "Spawned by literal command name via scripts/check-lint.mjs (checks/lint.ts's own script) -- nothing `import`s the package, so knip cannot see the use.",
      "kind": "unused-dev-dependency",
      "name": "oxlint"
    },
    {
      "id": "dead-code:unused-dev-dependency:publint",
      "version": 1,
      "justification": "Spawned by literal command name via the publint preset's `run: [\"publint\", \"run\"]` (src/presets/publint.ts) -- nothing `import`s the package, so knip cannot see the use.",
      "kind": "unused-dev-dependency",
      "name": "publint"
    },
    {
      "id": "dead-code:unused-dev-dependency:yaml",
      "version": 1,
      "justification": "Genuinely `import`ed (`import { parse as parseYaml } from \"yaml\"`) by scripts/openssf-scorecard/local-checks.ts, to parse .github/workflows/*.yml for the Pinned-Dependencies/Token-Permissions/Dangerous-Workflow sub-checks -- but that file is reachable only by being imported by scripts/openssf-scorecard/run.ts, itself reachable solely by being spawned via `tsx` from checks/openssf-scorecard.ts's `run: [...]`, so knip's static import graph never resolves the use (the same class of gap as local-checks.ts's own `unused-file` entry below). repo-contract's public output-format API dropped first-class YAML support (see src/types.ts's OutputFormat) -- this is `yaml`'s sole remaining, internal-tooling-only use, no longer a `peerDependencies` entry.",
      "kind": "unused-dev-dependency",
      "name": "yaml"
    },
    {
      "id": "dead-code:unused-file:scripts/iso-12207-alignment/gather-evidence.ts",
      "version": 1,
      "justification": "Imported only by scripts/iso-12207-alignment/run.ts, itself reachable solely by being spawned via `tsx` from checks/iso-12207-alignment.ts's `run: [...]` -- knip's static import graph never resolves a spawned-not-imported entry point, so this whole file's own reachability is invisible to it, the same class of gap as every scripts/openssf-scorecard/*.ts entry above.",
      "kind": "unused-file",
      "name": "scripts/iso-12207-alignment/gather-evidence.ts"
    },
    {
      "id": "dead-code:unused-file:scripts/iso-12207-alignment/run.ts",
      "version": 1,
      "justification": "The iso-12207-alignment check's own entrypoint, spawned by literal path via checks/iso-12207-alignment.ts's `run: [\"tsx\", \"scripts/iso-12207-alignment/run.ts\"]` -- nothing `import`s it, so knip cannot see the use, the same as every other check's own spawned run script (see e.g. scripts/openssf-scorecard/run.ts).",
      "kind": "unused-file",
      "name": "scripts/iso-12207-alignment/run.ts"
    },
    {
      "id": "dead-code:unused-file:scripts/openssf-scorecard/gh-api.ts",
      "version": 1,
      "justification": "Imported only by scripts/openssf-scorecard/run.ts and github-checks.ts, both themselves reachable solely by being spawned via `tsx` from checks/openssf-scorecard.ts's `run: [...]` -- knip's static import graph never resolves a spawned-not-imported entry point, so this whole file's own reachability is invisible to it, the same class of gap as every `unused-dev-dependency` entry above.",
      "kind": "unused-file",
      "name": "scripts/openssf-scorecard/gh-api.ts"
    },
    {
      "id": "dead-code:unused-file:scripts/openssf-scorecard/github-checks.ts",
      "version": 1,
      "justification": "Imported only by scripts/openssf-scorecard/run.ts, itself reachable solely by being spawned via `tsx` from checks/openssf-scorecard.ts's `run: [...]` -- see gh-api.ts's identical entry above.",
      "kind": "unused-file",
      "name": "scripts/openssf-scorecard/github-checks.ts"
    },
    {
      "id": "dead-code:unused-file:scripts/openssf-scorecard/local-checks.ts",
      "version": 1,
      "justification": "Imported only by scripts/openssf-scorecard/run.ts, itself reachable solely by being spawned via `tsx` from checks/openssf-scorecard.ts's `run: [...]` -- see gh-api.ts's identical entry above.",
      "kind": "unused-file",
      "name": "scripts/openssf-scorecard/local-checks.ts"
    },
    {
      "id": "dead-code:unused-file:scripts/openssf-scorecard/run.ts",
      "version": 1,
      "justification": "The openssf-scorecard check's own entrypoint, spawned by literal path via checks/openssf-scorecard.ts's `run: [\"tsx\", \"scripts/openssf-scorecard/run.ts\"]` -- nothing `import`s it, so knip cannot see the use, the same as every other check's own spawned run script (see e.g. scripts/security-socket/scan.ts, scripts/dead-code/check.ts).",
      "kind": "unused-file",
      "name": "scripts/openssf-scorecard/run.ts"
    },
    {
      "id": "dead-code:unused-type:GhApiResult",
      "version": 1,
      "justification": "The return type of ghApiJson() (gh-api.ts), consumed structurally by every caller in github-checks.ts without ever naming the type directly (each caller narrows `.ok` and reads `.value`/`.message` rather than annotating a variable with `GhApiResult<T>`) -- genuinely used as a type, just never referenced by name outside the file that declares it.",
      "kind": "unused-type",
      "name": "GhApiResult"
    }
  ]
}

node_modules
dist
coverage
reports
.stryker-tmp
test/e2e/*/fixtures
*.tgz

# Generated/derived, not authored: already covered by scanning the source
# they're derived from. Also keeps the JSON report's embedded sourceContent
# bounded -- history.json is scripts/run-contract.mjs's own unbounded,
# ever-growing local run log (gitignored, never committed) and previously
# ballooned reports/secretlint.json to tens of MB by itself; the
# .repo-contract/api-contract/current.* files are ephemeral build output
# re-derived from dist/ on every run (see .gitignore's own comments on both).
history.json
.repo-contract/api-contract/current.*

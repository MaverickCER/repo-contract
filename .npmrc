# Always attach a provenance attestation on publish, regardless of whether
# `npm publish` is invoked directly or wrapped by `changeset publish`. With
# OIDC trusted publishing (see .github/workflows/release.yml) this is
# attached automatically anyway, but setting it here is a harmless, explicit
# safety net -- same convention as env-cap/data-cap's own .npmrc.
provenance=true

# Always attach a provenance attestation on publish. With OIDC trusted
# publishing (see .github/workflows/release.yml's `publish` job) this is
# attached automatically anyway, but setting it here is a harmless, explicit
# safety net -- same convention as env-cap/data-cap's own .npmrc.
provenance=true

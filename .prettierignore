dist
coverage
node_modules
schemas/*.schema.json
scripts/suppression-governance/disable-comments.schema.json
disable-comments.json
CHANGELOG.md
package-lock.json
test/e2e/*/fixtures
reports
# Machine-generated by the api-contract check's own tooling -- same
# rationale as eslint.config.js's own `.repo-contract` ignore entry:
# baseline.api.json/baseline.d.ts are hash-verified against
# baseline.meta.json (see scripts/api-contract/baseline-store.ts), and
# current.* is regenerated every run. Reformatting either would corrupt the
# very snapshot compatibility diffing depends on.
.repo-contract
# Machine-generated by API Extractor (scripts/api-docs/) and compared
# byte-for-byte against a fresh, never-Prettier-formatted regeneration (see
# scripts/api-docs/check.ts's `committedContent === report.content`) --
# reformatting the committed copy makes it permanently "out of date" from
# the api-docs check's point of view.
docs/api-report

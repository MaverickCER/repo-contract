dist
coverage
node_modules
# POSIX shell hook scripts -- Prettier has no shell parser.
.githooks
# Git commit message template (wired by scripts/install-hooks.mjs) -- plain
# text with `#` comment lines, not a format Prettier parses.
.gitmessage
schemas/*.schema.json
scripts/suppression-governance/disable-comments.schema.json
CHANGELOG.md
package-lock.json
test/e2e/*/fixtures
# examples/ is a self-contained demonstration workspace with its own Prettier
# config (see examples/README.md) -- this repo's `format` check must not resolve
# that config against the example tree or couple its result to files it doesn't
# own. The examples are kept Prettier-clean under their own toolchain.
examples
reports
# Tooling-owned, never hand-formatted -- same rationale as eslint.config.js's
# own `.repo-contract` ignore entry. api-contract/: baseline.api.json/
# baseline.d.ts are hash-verified against baseline.meta.json (see
# scripts/api-contract/baseline-store.ts) and current.* is regenerated every
# run -- reformatting either corrupts the snapshot compatibility diffing
# depends on. exceptions/: the reviewed-exception registries
# (disable-comments.json is deterministically serialized by
# scripts/suppression-governance/check.ts and asserted byte-stable by its
# real-source integration test; socket.json/coderabbit.json/
# security-network.json are hand-authored to their own registry.ts contracts).
.repo-contract
# Machine-generated by API Extractor (scripts/api-docs/) and compared
# byte-for-byte against a fresh, never-Prettier-formatted regeneration (see
# scripts/api-docs/check.ts's `committedContent === report.content`) --
# reformatting the committed copy makes it permanently "out of date" from
# the api-docs check's point of view.
docs/api-report
# Machine-generated by scripts/api-docs-html/ (release-cadence only, via
# .github/workflows/api-baseline.yml -- see that file and specs/decisions/
# 0008-self-hosting-tool-and-dependency-choices.md's amendment). Same
# reasoning as docs/api-report above: Prettier reformatting the committed
# HTML would put it in permanent, spurious conflict with the generator.
docs/api
